# RTL-SDR Node Environment Configuration

# Node Identification
RTLSDR_NODE_ID=rtlsdr_main

# RTL-SDR Radio Configuration
RTLSDR_FREQUENCY=146.72e6        # Frequency in Hz (146.72 MHz = 2m band repeater)
RTLSDR_SAMPLE_RATE=240000        # Sample rate in Hz (240 kHz recommended for FM voice)
RTLSDR_GAIN=20.0                 # Receiver gain in dB (0.0 to 49.6, or "auto")
RTLSDR_SQUELCH=-40.0             # Squelch threshold in dBFS (-50.0 sensitive, -30.0 less sensitive)
RTLSDR_SQUELCH_DELAY=2.0         # Seconds of silence before stopping recording

# Recording Configuration
RTLSDR_OUTPUT_DIR=/recordings    # Local directory for recordings (container path)

# Web Control Panel
RTLSDR_WEB_PORT=8585             # Web UI port (default 8585)

# SFTP Server Configuration
SFTP_HOST=192.168.0.104          # TSN server IP or hostname
SFTP_PORT=22                     # SSH port
SFTP_USERNAME=tsn                # SSH username
SFTP_PASSWORD=your_password_here # SSH password (or leave empty if using key)
SFTP_KEY_FILE=                   # SSH private key path (e.g., /root/.ssh/rtlsdr_key)
SFTP_REMOTE_DIR=/incoming        # Remote directory on TSN server
SFTP_DELETE_AFTER_UPLOAD=true   # Delete local file after successful upload
SFTP_SCAN_INTERVAL=10            # Seconds between directory scans for new recordings

# Common Amateur Radio Frequencies (examples)
# 2 Meter Band (VHF)
# 146.52e6  - National simplex calling frequency
# 146.72e6  - Repeater output example
# 145.50e6  - Repeater input example

# 70 cm Band (UHF)  
# 446.00e6  - UHF repeater
# 433.50e6  - UHF simplex
